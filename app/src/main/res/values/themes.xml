<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CodeScannerGenerator" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/black</item>
        <item name="android:statusBarColor"  >?android:attr/colorPrimary</item>
        <item name="android:navigationBarColor" >?android:attr/colorPrimary</item>
    </style>

    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="colorPrimary">@color/bg_input_edit_text</item>
        <item name="hintTextColor">@color/brown</item>
        <item name="android:textColorHint">@color/brown</item>
        <item name="boxStrokeColor">@color/bg_input_edit_text</item>
        <item name="boxStrokeWidth">@dimen/_1sdp</item>
        <item name="colorControlActivated">@color/brown</item>
        <item name="endIconTint">@color/brown</item>
    </style>

    <style name="Theme.CodeScannerGenerator" parent="Base.Theme.CodeScannerGenerator" />
</resources>